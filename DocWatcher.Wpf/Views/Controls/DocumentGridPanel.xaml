<UserControl x:Class="DocWatcher.Wpf.Views.DocumentGridPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="400"
             d:DesignWidth="450">

    <UserControl.Resources>
        <!-- Stile per il bottone "Cerca" -->
        <Style x:Key="SearchButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="10,0,0,0"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Filtri -->
            <RowDefinition Height="*"/>
            <!-- Griglia -->
        </Grid.RowDefinitions>

        <ComboBox x:Name="CmbFilterType"
            Grid.Row="0"
            Grid.Column="0"
            SelectedIndex="0"
            MinWidth="220" Width="Auto" VerticalContentAlignment="Center" SelectionChanged="BtnSearch_Click" />

        <GroupBox Header="Documenti" Grid.Row="1" Margin="0,8,0,0" Padding="5">
        </GroupBox>

        <!-- GRIGLIA DOCUMENTI -->
        <GroupBox Header="Documenti" Grid.Row="1" Margin="0,8,0,0" Padding="5">
            <DataGrid x:Name="DocumentsGrid"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      SelectionMode="Single"
                      ItemsSource="{Binding ItemsSource,
                                            RelativeSource={RelativeSource AncestorType=UserControl}}"
                      SelectedItem="{Binding SelectedItem,
                                             RelativeSource={RelativeSource AncestorType=UserControl},
                                             Mode=TwoWay}"
                      SelectionChanged="DocumentsGrid_SelectionChanged">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Titolo"
                                        Binding="{Binding Titolo}"
                                        Width="*" />
                    <DataGridTextColumn Header="Scadenza"
                                        Binding="{Binding DataScadenza, StringFormat=dd/MM/yyyy}"
                                        Width="150" />
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
    </Grid>
</UserControl>
